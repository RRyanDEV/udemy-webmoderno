{
    {
        {
            {
                var sera = 'Será?????'
            }
        }
    }
}
console.log(sera)

function teste(){
    var local = 123
    console.log(local)
} // Nesse caso a variavel só fica visivel dentro da função.

// Variável só tem dois escopos possiveis, nivel global(visivel no código inteiro) ou escopo de função(que fica disponivel apenas dentro da função)