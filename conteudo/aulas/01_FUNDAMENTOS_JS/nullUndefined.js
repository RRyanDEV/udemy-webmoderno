let valor // Não Inicializada
console.log(valor); // O valor por padrão dela é undefined, pois ele nem foi iniciada.

valor = null // Ausência de valor
console.log(valor) // O null serve para tirar o apontamento de uma variável.