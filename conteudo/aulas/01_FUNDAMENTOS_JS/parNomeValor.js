// par nome/valor

const saudacao = 'Opa' 

function exex(){
    const saudacao = 'Falaa'
    return saudacao
};

// Objetos são grupos aninhados de pares de nome/valor
const cliente = {
    nome: 'Pedro',
    idade: 20,
    peso: 90,
};

console.log(saudacao);
console.log(exex());
console.log(cliente)