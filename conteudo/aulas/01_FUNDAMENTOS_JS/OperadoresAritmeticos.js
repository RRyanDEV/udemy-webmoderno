const [a, b ,c ,d] = [100, 20, 700, 30]

const soma = a + b + c + d
const subtracao = d - b
const multiplicacao = a * b
const divisao = d / b
const modulo = d % 4

const result = {
    soma,
    subtracao,
    multiplicacao,
    divisao,
    modulo,
}

console.log(result);
